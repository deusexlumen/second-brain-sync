#!/usr/bin/env python3
"""
Skill: commands
Description: Zeigt alle verfügbaren ! Befehle an
Usage: !commands
"""

print("""📜 **Verfügbare Befehle**

**Jeder kann nutzen:**
```
!tts [Text]       → Text-to-Speech (KI-Stimme)
!kaomoji          → Zufälliges Kaomoji
!kaomoji evil     → Gefiltert nach Tag (evil, hug, heart, etc.)
!analyse          → Logische Fehlschlüsse identifizieren
!roast            → Systemtheoretische Dekonstruktion 🔥
!tarot            → Archetypen-Mapping
!atmosphere       → Wetter als kollektive kognitive Latenz
!status           → System-Status anzeigen
!help             → Diese Hilfe
```

**Nur Admins (Owner):**
```
/config           → Konfiguration ändern
/restart          → Gateway neustarten
/bash             → Shell-Zugriff
/debug            → Debug-Modus
```

**Beispiele:**
• `!tts Hallo Welt` → Generiert KI-Sprache
• `!kaomoji hug` → Zeigt Umarmungs-Kaomojis
• `!kaomoji anger` → Zeigt wütende Kaomojis
• `!analyse [Text]` → Analysiert auf logische Fehler

*Resonanz ist das Einzige, was zählt.*
""")
